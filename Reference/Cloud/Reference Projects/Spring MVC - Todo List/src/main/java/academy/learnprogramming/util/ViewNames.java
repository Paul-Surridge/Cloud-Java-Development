package academy.learnprogramming.util;

public final class ViewNames {

    // == constants ==
    // These constant correspond and therefore must match the names of the *.jsp files that are called.
    public static final String ITEMS_LIST = "items_list";
    public static final String ADD_ITEM = "add_item";
    public static final String VIEW_ITEM = "view_item";
    public static final String HOME = "home";

    // == constructors ==
    private ViewNames() {}       //Ensure constructor is private to prevent instantiation of this class (as it is only used to store constants).
}
