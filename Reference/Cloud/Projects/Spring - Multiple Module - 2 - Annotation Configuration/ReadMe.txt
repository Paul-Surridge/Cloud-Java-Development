Project Items:

    - Annotation Configuration (rather than XML configuration)
    - @Configuration annotation
    - @ComponentScan annotation
    - @Bean annotation
    - Configuration Class i.e. a separate class to hold all of the container configuration metadata (instead of an *.xml).